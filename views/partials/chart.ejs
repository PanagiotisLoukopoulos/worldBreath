<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>World Breath</title>
    <link rel="stylesheet" href="/css/styles.css">


    <!-- Font Awesome -->
    <script src="https://kit.fontawesome.com/9ee398e75f.js" crossorigin="anonymous"></script>
    <!-- chart -->
    
</head>
<body>

    <section class="nav-section" id="title">
            <!-- Logo, Icon Bar -->
            <div>
                <div class="my-logo"><b>WorldBreath</b></div>
                    <div class="icon-bar ">
                
                        <!-- <a href="/home" class=""><i class="fa-solid fa-house"></i></a>  -->
                        <!-- <a href="/dashboard"><i class="fa-solid fa-chart-simple"></i></a> -->
                        <!-- <a href="/qrCode"><i class="fa-solid fa-qrcode"></i></a> -->
                        <!-- <a href="/footprint"><i class="fa-solid fa-leaf"></i></a>  -->
                        <!-- <a href="/profile" class="active"><i class="fa-solid fa-circle-user"></i></a> -->
                        <!-- <a href="/logout" role="button"><i class="fa-solid fa-circle-user"></i></a>  -->
 
                    </div>
            </div>    
    </section>
<!-- CONTAINER FOR CHART Enallaktiko chart gia dashboard-fireplace

<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<body>
<div id="myChart" style="width:100%; max-width:600px; height:500px;padding-top: 15rem;"></div>

<script>
google.charts.load('current',{packages:['corechart']});
google.charts.setOnLoadCallback(drawChart);

var y = '%=eCO2_ccs811%>'; (exw vgalei to brosta <)
var x = '%=date_ccs811%>';
y = Number(y);

function drawChart() {

// Set Data
let data = google.visualization.arrayToDataTable([
  ['Date', 'co2'],
  ["",0]
]);
// Set Options
let options = {
  title: 'Firplace emmision Co2',
  hAxis: {title: 'Date'},
  vAxis: {title: 'CO2 ppm'},
  legend: 'none'
};
// Draw
var chart = new google.visualization.LineChart(document.getElementById('myChart'));
chart.draw(data, options);

setInterval(function() {
  //alert(x)
  //alert(typeof(y))
  			data.addRow([x,y]);
  			chart.draw(data, options);
  			
			}, 5000);
}
</script> -->

    
<!-- CONTAINER FOR CHART
<div id="chart_div" style="width:100%; max-width:600px; height:500px;padding-top: 15rem;"></div>
<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<script>
    // load current chart package
    google.charts.load("current", {
        packages: ["corechart", "line"]
    });
    // set callback function when api loaded
    google.charts.setOnLoadCallback(drawChart);

    var y = '%=eCO2_ccs811%>';
    var x = '%=date_ccs811%>';
    y = Number(y);

    function drawChart() {
        // create data object with default value
        let data = google.visualization.arrayToDataTable([
            ["Year", "Co2"],
            [0, 0]
        ]);
        // create options object with titles, colors, etc.
        let options = {
            title: "CO2 emissions",
            hAxis: {
                title: "Time"
            },
            vAxis: {
                title: "CO2"
            }
        };
        // draw chart on load
        let chart = new google.visualization.LineChart(
            document.getElementById("chart_div")
        );
        chart.draw(data, options);
        // interval for adding new data every 250ms
        let index = 0;
            setInterval(function() {
                // instead of this random, you can make an ajax call for the current cpu usage or what ever data you want to display
                
                data.addRow([index, y]);
                chart.draw(data, options);
                index++;
            }, 250); -->
}
    
    </script>
    